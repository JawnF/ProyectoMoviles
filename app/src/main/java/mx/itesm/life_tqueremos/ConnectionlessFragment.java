package mx.itesm.life_tqueremos;


import android.os.Bundle;
import android.support.annotation.Nullable;
import android.support.v4.app.Fragment;
import android.view.LayoutInflater;
import android.view.View;
import android.view.ViewGroup;


/**
 * A simple {@link Fragment} subclass.
 */
public class ConnectionlessFragment extends Fragment {


    public ConnectionlessFragment() {
        // Required empty public constructor
    }

    public static ConnectionlessFragment newInstance() {
        Bundle args = new Bundle();

        ConnectionlessFragment fragment = new ConnectionlessFragment();
        fragment.setArguments(args);
        return fragment;
    }

    @Override
    public void onCreate(@Nullable Bundle savedInstanceState) {
        super.onCreate(savedInstanceState);
        if (getArguments() != null) {
        }
    }

    @Override
    public View onCreateView(LayoutInflater inflater, ViewGroup container,
                             Bundle savedInstanceState) {
        // Inflate the layout for this fragment
        return inflater.inflate(R.layout.fragment_connectionless, container, false);
    }

}
